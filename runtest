#!/usr/bin/env ruby

if ARGV.length < 4
  print("runtest TEST_NAME PKG_RPM NUM_ITEMS NUM_VBUCKETS [JOIN_URL] [EXTRA_SLEEP]\n")
  exit(-1)
end

print "runtest #{ARGV}\n"

$test_name    = ARGV[0]
$pkg          = ARGV[1]
$num_items    = ARGV[2]
$num_vbuckets = ARGV[3]
$join_url     = ARGV[4]
$extra_sleep  = ARGV[5]

`mkdir -p out`
$out_file = "out/#{$test_name}"
`rm -f #{$out_file}`

$time_curr = $time_start = Time.now
$time_prev = nil

def next(msg, cmd=nil)
  prev = curr
  curr = Time.now

  `echo "# #{msg}" >> #{$out_file}`
  `echo "#{curr.strftime('%Y%m%d-%H%M%S')} (#{curr - prev})" >> #{$out_file}`
end

def time(cmd)
  system("top -u couchbase | egrep beam.smp > /tmp/top.out &")

  `#{cmd}`

  `killall top`
  `tail -n 50 /tmp/top.out | head -n 10 >> #{$out_file}`
end

next("starting... runtest #{test_name}")

`killall -9 memcached || true`

`/etc/init.d/couchbase-server stop || true`

`rpm -e couchbase-server || true`
`rm -rf /opt/couchbase/`

next("installing...")

`rpm -i #{$pkg}`

sleep 5
sleep $extra_sleep if $extra_sleep

`rm -f after-install`
`wget https://raw.github.com/couchbaselabs/rampup/master/after-install`
`chmod a+x after-install`
`./after-install #{$num_vbuckets}`

next("configuring...")

if $join_url
  my_ip = `/sbin/ifconfig eth0|grep inet|awk {'print $2'}|cut -d":" -f2|head -n 1`
  `/opt/couchbase/bin/membase rebalance -c #{$join_url} \
    -u Administrator -p password \
    --server-add=#{my_ip}`
else
  `/opt/couchbase/bin/membase cluster-init -c 127.0.0.1 \
    --cluster-init-username=Administrator \
    --cluster-init-password=password`
  `/opt/couchbase/bin/membase bucket-create -c 127.0.0.1 \
    -u Administrator -p password \
    --bucket=default --bucket-type=membase --bucket-password= \
    --bucket-ramsize=6440 --bucket-replica=0`
end

sleep 5
sleep $extra_sleep if $extra_sleep

`curl -vX PUT http://127.0.0.1:5984/default/_design/rampup -d @rampup.json`

next("loading-membase...")
time(<<EOF
     DRIVE_HOST=127.0.0.1 RATIO_SETS=1.0 RATIO_CREATES=1.0 MAX_CREATES=$3 EXIT_AFTER_CREATES=1 \
       /opt/couchbase/bin/erl -pa . /opt/couchbase/lib/ns_server/erlang/lib/ns_server-*/ebin \
       -eval 'rampup:drive_test(), init:stop().'
EOF
)

next("using-membase...")
time(<<EOF
     DRIVE_HOST=127.0.0.1 RATIO_SETS=0.0 MAX_OPS=10000 \
       /opt/couchbase/bin/erl -pa . /opt/couchbase/lib/ns_server/erlang/lib/ns_server-*/ebin \
       -eval 'rampup:drive_test(), init:stop().'
EOF
)

next("view-building...")
time("/usr/bin/time -a -o #{$out_file} curl -v http://127.0.0.1:5984/default/_design/rampup/_view/random?limit=10")

next("view-accessing...")
time("./rampup-view.rb 127.0.0.1 10000 >> #{$out_file}")

next("done... runtest #{test_name} (#{Time.now - time_start})")
