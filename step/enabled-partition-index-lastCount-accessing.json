{ "_id": "step_enabled-partition-index-lastCount-accessing",
  "kind": "step",
  "description": "With partition 0 re-enabled, access the view 10,000 times, sequentially, via a greater-than-or-equal query, limit 10 results per query."
}
