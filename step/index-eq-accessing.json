{ "_id": "step_index-eq-accessing",
  "kind": "step",
  "description": "Access the last (high-fan-out) index 10,000 times, sequentially, via a equal-to query, limit 10 results per query.  Against couch, that means a startkey is the same as the endkey."
}
