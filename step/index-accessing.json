{ "_id": "step_index-accessing",
  "kind": "step",
  "description": "Access the last (high-fan-out) index 10,000 times, sequentially, via a greater-than-or-equal query, limit 10 results per query. Against couch, that means a startkey but no endkey."
}
