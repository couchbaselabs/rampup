{ "_id": "step_index-accessing2",
  "kind": "step",
  "description": "Access the last (high-fan-out) index 10,000 times, sequentially, via a greater-than-or-equal query, limit 10 results per query, but do it again a second time to see if system caching has an effect."
}
