{ "_id": "step_index-mid-accessing2",
  "kind": "step",
  "description": "Access the mid (low-fan-out) index 10,000 times, sequentially, via a greater-than-or-equal query, limit 10 results per query, a second time to see if system caching has an effect."
}
