{ "_id": "step_index-mid-eq-accessing",
  "kind": "step",
  "description": "Access the mid (low-fan-out) index 10,000 times, sequentially, via a equal-to query, limit 10 results per query. Against couch, that means a startkey but no endkey."
}
