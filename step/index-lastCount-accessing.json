{ "_id": "step_index-lastCount-accessing",
  "kind": "step",
  "description": "Access the lastCount index 10,000 times, sequentially, via a greater-than-or-equal query, limit 10 results per query. Against couch, that means a startkey but no endkey."
}
